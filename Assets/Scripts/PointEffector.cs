using UnityEngine;

public class PointEffector : MonoBehaviour
{
    //force
    //radius (slider)

    //push if positive, pull if negative
    //two versions 

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
